# Installation

Full installation instructions will be added in Milestone 2.

The planned container creation flow is:

```bash
distrobox create \
  --name unity-dev \
  --image ubuntu:26.04 \
  --nvidia
```
